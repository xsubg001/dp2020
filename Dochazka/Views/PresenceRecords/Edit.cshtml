@model Dochazka.Models.PresenceRecord

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>PresenceRecord</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WorkDay" />
            <input type="hidden" asp-for="DayTimeSlot" />
            <input type="hidden" asp-for="employeeId" />
            <div class="form-group">
                <label asp-for="Presence" class="control-label"></label>
                <select asp-for="Presence" class="form-control"></select>
                <span asp-validation-for="Presence" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ManagerApprovalStatus" class="control-label"></label>
                <select asp-for="ManagerApprovalStatus" class="form-control"></select>
                <span asp-validation-for="ManagerApprovalStatus" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
